{
  "name": "bookworm-app",
  "version": "1.0.0",
  "description": "BookWorm App - Full Stack Application",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm install",
    "heroku-postbuild": "cd backend && npm install"
  },
  "keywords": ["bookworm", "books", "fullstack"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}